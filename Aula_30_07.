def crla_lista():
    # variavel 'numeros' vaí  para a strack
    # a listac [1,2,3] vai para o Help
    numeros = [1,2,3]
    return numeros 

    resultado = credits()
    # a função acabou, mas a lista 
    # ainda exite no Healp!
    print(resultado) # {1,2,3}
