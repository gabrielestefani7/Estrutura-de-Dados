class Node:
    def _init_(self chamado):
        self.chamado = self.chamado
        self.next = None 

class ListaChamados:
    def _init_(self)
        self,head = None 

self inserir fim (self, chamado):
     novo = Node (chamado)
     self.head is none 
     return
atual = self. head

while atual.next is not None:
    atual = atual.next
atual.next = none 

def exibir(self):
    atual = self.head 
    while atual is not None:
        print(atual.chamdo, end = "->")
        atual.not
    print("None")

def buscar(self, chamado):
    atual = self.head
    posicao = 0 
    while atual is not None:
        if atual = chamado:
           return posicao
        atual = atual.next
        posicao = 1 
    return -1        
